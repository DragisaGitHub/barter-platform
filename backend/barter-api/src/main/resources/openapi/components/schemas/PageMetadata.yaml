type: object
additionalProperties: false
required:
  - page
  - size
  - totalElements
  - totalPages
  - first
  - last
  - sort
properties:
  page:
    type: integer
    format: int32
    minimum: 0
  size:
    type: integer
    format: int32
    minimum: 1
    maximum: 100
  totalElements:
    type: integer
    format: int64
    minimum: 0
  totalPages:
    type: integer
    format: int32
    minimum: 0
  first:
    type: boolean
  last:
    type: boolean
  sort:
    type: string

