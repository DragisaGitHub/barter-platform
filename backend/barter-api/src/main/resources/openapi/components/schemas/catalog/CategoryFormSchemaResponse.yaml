type: object
additionalProperties: false
required:
  - categoryUuid
  - fields
properties:
  categoryUuid:
    type: string
    format: uuid
  schemaUuid:
    type: string
    format: uuid
    nullable: true
  schemaVersion:
    type: integer
    format: int32
    nullable: true
  schemaStatus:
    allOf:
      - $ref: '../../../openapi.yaml#/components/schemas/CategorySchemaStatus'
    nullable: true
  fields:
    type: array
    items:
      $ref: '../../../openapi.yaml#/components/schemas/CategoryFormFieldResponse'

