spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/barter_db
    username: barter_user
    password: barter_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

  # ── Mailpit (local SMTP catch-all) ──────────────────────────────────────────
  # Start with:  docker compose up -d mailpit
  # Inspect at:  http://localhost:8025
  mail:
    host: localhost
    port: 1025
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
            required: false

barter:
  jwt:
    # Local-only fallback so the profile remains usable without exporting JWT_SECRET.
    # DEV/PROD should still provide JWT_SECRET explicitly via env/config.
    secret: ${JWT_SECRET:local-dev-only-jwt-secret-32-plus-chars-for-barter-platform}
  mail:
    from: noreply@barter-platform.local
  security:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
      - http://localhost:4173
      - http://127.0.0.1:4173
  storage:
    type: local
    local:
      base-path: uploads/

logging:
  level:
    org.hibernate.SQL: off
    org.hibernate.orm.jdbc.bind: off
    org.springframework.security: info
    com.barterplatform: debug